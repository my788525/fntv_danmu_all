# 飞牛影视 TV · BYD 车机专版 — 修改说明

本仓库是 [`jimboo7339/fntv_danmu_all`](https://github.com/jimboo7339/fntv_danmu_all) 的 **BYD 车机专版分支**（包名 `com.fntv.byd`，pubspec name `fnos_tv_byd`）。

以下为相对原作（上游 `main`）的全部主要修改与新增功能，按模块归类。

---

## 一、基础与可用性修复

- **修复播放黑屏（Authx 拉流签名缺失）**：原作 `api_client` 缺少 Authx 签名头，导致 `media/range` 拉流请求返回 403、播放黑屏。已补全 `mediaHeaders`，拉流恢复正常。
- **修复媒体库只能查看 50 个影视的限制**：`item/list` 的 `ancestor_guid` 分支修正翻页逻辑（按 `total` 取满全部分页），不再被服务端单页上限截断；并修正回退参数 `exclude_grouped_video`。
- **配置 release 正式签名**：使用独立签名密钥（路径经 `.gitignore` 不入库），保证包签名稳定。
- **修复白屏**：`namespace` 必须匹配 Kotlin 源码包。
- **在线更新切到自有仓库**：更新检查指向 `my788525/fntv_danmu_all`，并提供多个国内 GitHub 镜像加速源（ghproxy / gh.llkk.cc 等），APK 资产名以 `FNTV_All_` 开头供 App 内识别。

---

## 二、媒体库文件夹层级浏览

- 进入媒体库改为**文件夹层级浏览**：按 `parent_guid` 逐级下钻，返回键逐级回上一级，库根返回媒体库列表。
- 解决 FnOS **虚拟文件夹**问题：音乐库等场景下，文件夹节点不出现在 `item/list` 的平铺结果里，只能从子项的 `parent_guid`（形如 `fv_xxx`）推断其存在；改用 `GET /api/v1/item/{guid}` 逐个解析出文件夹名称，合并进目录树。
- 根视图 = 库根直接视频 + 虚拟文件夹；**文件夹在前、视频在后**。

---

## 三、剧集选集页

- 新增**剧集选集页**：点剧集 / 季进入，多季时按季分 tab，单季直接出集数列表（列表式、**不显示海报**）。
- 每集卡含「第 N 集」编号、标题、**已看 ✓ 标记**、**续播进度条**。
- 点集进入播放器并传入**季 guid**，使播放器内选集面板与上一集 / 下一集生效。
- 修复多季切换卡在「加载中」不显示的 bug（取集后 `_loading` 未复位）。

---

## 四、自动连播

- 文件夹 / 剧集**播完自动续播下一个**，到末尾循环回首个。
- 修复原连播依赖轮询 `position >= duration` 猜测结束、末尾差一帧就永远不触发的问题：改用播放器原生 `completed` 事件（`media_kit` 的 `stream.completed` + ExoPlayer 单发检测），并加「每项只触发一次」保护，避免重复续播。

---

## 五、播放器「上一个 / 下一个 + 选集」切换

- 播放器控制条新增**「上一 / 下一」按钮**与**「选集」快选面板**，**文件夹播放与剧集播放共用同一套逻辑**：
  - 文件夹：同级可播放视频列表，编号 1/2/3… + 视频真实名称，当前项高亮。
  - 剧集：整季选集，当前集高亮。
- 点选集任意项即可跳转；上下项按钮随播放位置实时同步。

---

## 六、首页重构

- 移除底部「首页」tab，只保留**「媒体库」作为主入口 / 默认首屏**（保留「设置」）。
- 原首页的「最近观看 / 继续观看」横滑区块**移至「媒体库」页面最上方**；进入媒体库即拉取服务端播放记录，点卡片可直接续播。

---

## 七、屏幕方向适配（比亚迪车机）

- **修复真机上打开 App 强制旋转为竖屏、且不跟随车机横竖屏切换的问题**。
- 根因：Manifest 曾声明 `android:screenOrientation="nosensor"`。该值语义是**忽略方向传感器、锁死为设备默认方向**（并非「跟随系统」），在比亚迪车机上默认方向被解析为竖屏，导致打开即转竖屏、车机切横屏后 App 不跟随。
- 修复：**移除 `screenOrientation` 声明**（恢复 Android 默认 `unspecified` = 完全跟随车机系统当前横竖屏），全代码不调用 `setPreferredOrientations` 锁定方向；配合 `configChanges` 已声明 `orientation|screenSize|screenLayout|smallestScreenSize` 等，方向切换时 Flutter 不重建 Activity、布局经 `MediaQuery` 自适应。
- 符合比亚迪生态开放平台「应用市场审核指南」要求：**「应用宜能适配支持横竖屏布局，并支持横竖屏自动切换」**（DiLink 旋转屏车型可通过方向盘按钮 / 设置切换横竖屏）。
- 播放器内保留用户主动点击的「横屏 / 竖屏」切换按钮；退出播放器自动恢复为跟随系统。

---

## 八、低端车机 SoC 优化（高通 652 / Adreno 510）

- **禁用 Impeller 渲染引擎**（Manifest `io.flutter.embedding.android.EnableImpeller=false`）：Flutter 3.32 在 Android 10+ 默认走 Impeller-Vulkan，Adreno 510 的 Vulkan 驱动陈旧，在 Android 10+ 的 DiLink 车机上可能掉帧 / 花屏；显式禁用回退经典 Skia/OpenGL 路径（Android 10 以下引擎本就自动回退，此项覆盖少数 10+ 老机型）。
- **API 调试日志改为仅 debug 构建启用**：此前 release 包每次 API 请求都 `print` + 同步写 `/sdcard` 日志文件，在老 SoC 上是每次请求都有的无谓磁盘 I/O；debug 联调能力保留。
- **发行包改用 arm64-v8a 单架构 APK**（`--split-per-abi`）：实测体积 47.1MB → **15.2MB**，车机在线更新下载量降约 2/3。高通 652 为 ARMv8 架构，原生支持 arm64-v8a。

---

## 九、DiLink 3.0 车机深度适配（QCM6125 / Android 9）

- **倒车 / 摄像头覆盖时自动暂停**：倒车影像或 360 环视画面接管车机屏幕时自动暂停播放，解除后自动续播（覆盖期间手动暂停过则不强行续播）。检测采用双信号源：DiLink 3 挡位框架信号（挂 R 挡判定）+ App 生命周期兜底；设置页新增开关「倒车/摄像头覆盖时自动暂停」（默认开）。
- **播放内核快速切换**：播放界面顶部的内核信息标签变为可点按钮，单击即在 ExoPlayer ↔ MPV 之间无缝切换（断点续播、偏好持久化）；带 PGS/DVB 内嵌字幕的片源禁止切回 Exo。
- **方向盘按键切集**：方向盘上/下键（媒体键 87/88 与 DPAD 19/20 两种键值均支持）切换上一集/下一集，长按连发忽略，越界有提示。

---

## 版本

- 当前版本 `1.0.3`（pubspec `1.0.3+4`）。
- `v1.0.3`：DiLink 3.0 车机深度适配（详见「九、DiLink 3.0 车机深度适配」）。
- `v1.0.2`：低端车机 SoC（高通 652）优化（详见「八、低端车机 SoC 优化」），发行包改用 arm64 单架构。
- `v1.0.1`：修复比亚迪车机打开 App 强制竖屏且不跟随横竖屏切换（详见「七、屏幕方向适配」）。
- APK 资产名以 `FNTV_All_` 开头，供 App 内在线更新识别；`v1.0.2` 起资产为 arm64-v8a 单架构包。

> 注：原作上游 `jimboo7339/fntv_danmu_all` 的 `main` 分支**不含**本分支的 Authx 拉流签名修复；任何「回到上游基线 / 重新 fork」操作都会回退该修复导致播放黑屏，请始终基于本 `byd-main` 分支。
